if(format!="mixup"){
matrixResults<- ChemicalProperty(testAll)
testingResults <- ChemicalProperty(testAll_testing)

}else{
  matrixResults <- ChemicalProperty(testAll)
  # resResults <- ChemicalProperty(rest_seq)
}